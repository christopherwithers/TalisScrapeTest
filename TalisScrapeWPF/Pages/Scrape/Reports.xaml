<UserControl x:Class="TalisScrapeWPF.Pages.Scrape.Reports"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="A" Width="30*"/>
            <ColumnDefinition Width="70*" />
        </Grid.ColumnDefinitions>
        <ListView Name="LvReports" SelectionChanged="LvReports_OnSelectionChanged" Grid.Column="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Date" Width="120" DisplayMemberBinding="{Binding Key}" />
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Grid.Column="1" HorizontalAlignment="Left" Height="100" VerticalAlignment="Top" Width="100">
        <TextBlock>dasdassdsa</TextBlock>
        </StackPanel>

    </Grid>
</UserControl>
